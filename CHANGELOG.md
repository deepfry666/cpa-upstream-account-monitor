# 更新日志

## 0.5.3 - 2026-09-19

本补丁修复旧位置型账户迁移到稳定 ID 时的快照和历史迁移缺口，并将 UI 回归测试封装为可重复执行的构建目标。偏好和缓存格式仍为版本 `2`，从 `0.5.2` 可直接升级。

### 迁移修复

- 旧位置型 ID 只有在来源类别、规范化地址、来源标识或带密钥指纹唯一匹配，且未被标记为 `relink_required` 时，才同时迁移活动快照和该账户历史。
- 迁移后的历史按查询时间排序、去重并保留最多 100 条；活动快照以较新记录为准。
- 迁移后立即落盘，第一次启动、第二次启动、历史一致性以及 0.4.1 成套备份解密和回滚路径均有自动化测试。

### 测试工具

- 新增 `make ui-test`，自动寻找空闲端口、启动仓库静态服务并在退出时清理。
- Playwright 优先使用本机已有安装；缺失时通过固定的 `1.55.0` 版本临时解析，不把测试依赖提交到仓库。
- UI 套件继续覆盖桌面、平板、手机、键盘、CPAMP iframe 亮暗主题和迁移相关状态，共 34 项。

### 真实接口核对

- 新增脱敏真实接口验收记录，覆盖 NewAPI、Command Code、DeepSeek、Sub2API 和智谱 / Z.ai 的只读结果。
- 智谱 / Z.ai 仍记录为外部接口限制；没有可验证现金余额接口时不伪造金额。

## 0.5.2 - 2026-09-19

本补丁不修改运行时行为、配置格式或缓存格式，补齐发布门槛中的 Linux 目标架构真实加载检查。

### 发布验证

- `make package` 在生成 ZIP 前执行真实 `dlopen(RTLD_NOW)`，并检查 `cliproxy_plugin_init`、`cliproxyPluginCall`、`cliproxyPluginFree`、`cliproxyPluginShutdown` 四个必需导出符号。
- Linux AMD64 使用当前 x86_64 构建环境原生加载；Linux ARM64 使用 `aarch64-linux-gnu-gcc` 编译加载器，并通过 `qemu-aarch64-static` 加载目标动态库。
- Linux ARM64 环境缺少 QEMU 时，从当前 apt 软件源下载 `qemu-user-static` 到临时目录，不把额外二进制提交到仓库或发布包。
- Release 工作流继续使用既有 `make package` 步骤，因此两个架构任一加载失败都会阻止后续发布。

## 0.5.1 - 2026-09-19

本补丁完成 v0.5.0 发布后复核发现的两项回归修复。存储格式和偏好格式继续保持版本 `2`，从 `0.5.0` 升级不需要迁移数据。

### 阈值重算

- 修改周期额度阈值后，本地快照会基于原始指标重新计算 `ok`、`warning` 和 `critical`，降低阈值能够正确恢复为健康状态。
- 重新计算不会覆盖余额不足、字段查询失败、过期、禁用等非阈值告警。
- 整个过程只更新内存并重新落盘，不触发上游请求。

### 缓存恢复

- 缓存读取或解析失败时保留原文件和原有内存快照，恢复错误会持续记录。
- 恢复失败期间拒绝自动覆盖不可读缓存，避免一次保存抹掉可人工恢复的数据。
- 管理页状态栏显示“快照缓存恢复失败”及原因，并进入错误状态；成功重新读取后自动清除警告。

## 0.5.0 - 2026-09-19

本版本完成《上游账户监控：完整修复实施方案》的 A–E 批次代码修改和发布材料。外部接口限制单独记录，不将智谱 / Z.ai 现金余额列为已实现。

### 账户身份与配置

- 引入持久化稳定 `account_id`、来源标识、首次发现时间和查询修订号。
- CPA 重排、删除前置项、改备注、重启后不再复用位置型 ID；同域多 Key 保持独立。
- 旧位置型配置只有唯一可核验对应关系时自动迁移；有歧义或缺少依据的记录标记重新关联。
- PAT 迁移使用旧 ID 解密、新 ID 重加密，AAD 与稳定账户绑定。
- 批量供应商配置和阈值配置使用校验、副本提交、修订冲突检查和原子写盘；失败不半更新。
- PAT 编辑使用 `keep`、`replace`、`clear` 三态，旧歧义请求明确拒绝。

### 缓存、恢复与后台任务

- 快照缓存升级为格式版本 2，保存活动条目和每账户最多 100 条历史，支持原子写盘和目录同步。
- 缓存识别无版本 `entries/history` 和旧账户 map；未知未来版本或损坏文件不会覆盖原文件或当前内存。
- 启动先恢复缓存；过期判断改为基于 `last_success_at`、配置修订和 TTL。
- 增加可取消的后台调度、抖动、失败指数退避、最多 15 分钟间隔、并发上限和退出等待。
- 后台独立读取宿主凭证目录与静态 CPA 配置，并使用插件自有 HTTP 客户端，不长期复用请求级 `host_callback_id`。
- 增加异步刷新任务；当前账户、全部账户、重新加载使用不同请求，重复账户刷新合并并返回成功 / 失败 / 跳过。
- 所有网络请求贯穿 context、连接 / TLS / 响应头 / 响应体超时，单账户另有总预算；慢账户不阻塞其他账户或配置保存。

### 协议与数据语义

- 删除备注关键字对协议选择的影响；显式适配器、可信元数据和端点特征决定路由。
- 修正 NewAPI USD、CNY、TOKENS、CUSTOM 换算，缺参时保留原始 quota 并说明未换算。
- 修正智谱和 OpenCode 的百分比、窗口命名和顺序，数组或 map 顺序不再改变指标语义。
- 套餐到期与窗口重置分离；没有明确重置时间时显示未提供。
- 保留超额窗口、负余额和小额非零费用，进度条限制不改变告警状态。
- 增加币种和账户级现金阈值，账户覆盖高于币种默认。
- 上游过期或禁用状态优先于 `isValid=true`；取消监控与上游禁用分开。
- Command Code 只映射官方返回窗口，不再用硬编码套餐总额覆盖服务端 grant。
- 智谱现金余额明确标记为外部接口限制，并提供控制台入口。
- 自动与手动 Sub2API 共用解析路径；钱包、Key、订阅、计费、速率、Token、daily 和 models 使用一致状态模型。
- NewAPI 账户 PAT 与当前 Key 查询独立，任一失败不丢弃另一有效结果。
- 状态汇总使用确定严重度顺序；首次失败、部分失败、过期、字段组状态和结构化中文告警均可追踪。
- 结构化时间统一为 RFC3339，区分尝试时间、成功时间、字段组时间、重置时间和到期时间。
- 端点使用结构化路径规范化，保留反代部署前缀；内部 HTTP 默认拒绝，必须按账户显式开启。

### 管理页面

- 页面首屏重排，已连接时收起连接表单；账户详情优先显示金额、窗口、更新时间和刷新操作。
- 草稿、服务器状态、查询状态和选择状态分离；标签、账户、轮询和保存失败不会丢草稿。
- 历史绑定稳定账户和请求序列，迟到响应不能显示到其他账户。
- 告警可跨名称、类型和“仅需关注”筛选定位，并处理归档账户。
- 供应商配置在 390 / 768 宽度改为单账户展开编辑，增加长名称和中文错误布局处理。
- 详情分为当前、计费与限制、用量统计、历史和诊断；原有字段和统计入口保留。
- 数据状态与监控设置分离；可编辑阈值持久化，只读配置明确来源。
- 刷新、保存、清除和连接提供进行中 / 成功 / 失败反馈；清除记录需二次确认。
- 增加平板布局、键盘可见焦点、CPAMP iframe 亮 / 暗主题和移动端交互回归测试。

### 发布与文档

- 版本更新为 `0.5.0`，README、英文 README、产品说明、设计说明、发布说明、迁移回滚和测试矩阵同步实际行为。
- Linux AMD64 / ARM64 分别生成版本化 ZIP、`.sha256` 和 `checksums.txt`，保留 `upstream-monitor.so` 文件名。
- 发布材料不包含 `dist/`、生成头文件、真实 PAT、密钥、账户响应、生产缓存或敏感截图。

### 已知外部限制

- 智谱 / Z.ai：官方没有当前可验证的公开现金余额查询接口；插件提供明确的“不支持自动查询”状态和控制台入口，不伪造金额。详见第 09 项测试矩阵。
