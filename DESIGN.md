# 上游账户监控设计系统

## Product Surface

CPA / CPA Manager Plus 的上游监控管理页。用户在桌面进行高频检查，在平板和手机上完成快速确认、修改备注、切换协议、输入 PAT、刷新和清除等操作。页面可能嵌入 CPAMP iframe，因此必须同时适配宿主亮色与暗色主题。

## Visual Direction

克制的运维控制台：内容优先、边界清晰、状态明确。使用冷白或主题表面色作为基础，蓝色用于主动作，绿 / 琥珀 / 红 / 紫仅用于状态。避免营销式大卡片、装饰性渐变、纯颜色状态和只靠 hover 才能发现的信息。

## Color Tokens

```text
Canvas          #f7f9fc
Surface         #ffffff
Muted surface   #f1f4f8
Border          #d8dee8
Ink             #172033
Muted ink       #617087
Accent          #315fd3
Success         #147d55
Warning         #a96800
Danger          #b42318
Unknown         #6b5ca5
```

暗色 iframe 使用对应主题变量覆盖，不依靠反色滤镜。状态既显示文字也显示颜色；金额、百分比和计数使用等宽数字特性。

## Typography

使用系统无衬线字体，中文优先。正文 14px，辅助信息 11–13px，页面标题 26px。金额、额度和百分比必须使用 `font-variant-numeric: tabular-nums`，刷新时不产生明显宽度跳动。长账户名允许换行，工具按钮和状态徽标不允许被长名称挤出容器。

## Information Architecture

```text
上游账户监控       连接状态       刷新全部 / 连接设置
账户概况与紧凑告警摘要
账户监控 | 供应商配置 | 监控设置 | 数据状态
```

### 账户监控

- 桌面：左侧为搜索、筛选和稳定排列的账户导航，右侧为详情。
- 窄屏：账户选择器显示名称、状态和主指标，详情位于同一纵向流程。
- 详情顶部固定“当前额度”，优先显示现金、账户额度、当前 Key 和 5 小时 / 一周 / 一月窗口。
- 详情标签为“计费与限制、用量统计、历史、诊断”，原有计费、倍率、速率限制、Token、每日和模型统计必须有明确入口。
- 历史标题包含账户名和稳定 ID；加载、空数据、失败均在详情内就地显示。

### 供应商配置

桌面保留紧凑表格，身份列保持可比较；窄屏改为摘要列表和单账户展开编辑。备注、来源、监控开关始终可见。选择 NewAPI 后立即显示 PAT 字段，不需要先保存。保存区、放弃修改和清除记录有明确边界；清除需要显示账户名和删除范围并二次确认。

### 监控设置

只包含运行时可持久化的周期百分比阈值和币种现金阈值。账户覆盖在供应商编辑区。字段缺失时显示“未配置”，`0` 是合法金额。校验规则为 `0 <= critical <= warning`，负余额作为明确异常处理。

### 数据状态

只读展示采集周期、单请求超时、单账户预算、缓存 TTL、目录最近尝试 / 成功时间和配置来源。由 CPA 配置文件控制且不支持运行时写回的字段不提供无效保存按钮。

## State And Interaction

页面状态分为：

- 服务器状态：报告、目录、配置和刷新任务。
- 草稿状态：按稳定账户 ID 保存的备注、协议、PAT、阈值和监控开关。
- 查询状态：刷新任务、历史请求和其他异步请求。
- 选择状态：当前标签、账户、筛选、排序和详情分组。

后台轮询只合并服务器状态，不覆盖脏字段。切换标签或账户保留草稿；保存使用 `base_revision`，冲突时保留草稿并提示重新核对。PAT 草稿只存在当前页面内存，不写 `localStorage`、`sessionStorage` 或 URL；仅在存在未保存草稿时拦截离开页面。

刷新按钮包含图标和可读文本，忙碌时防止重复任务。刷新任务显示成功、失败和跳过数量；保存成功提示不能被随后的状态轮询覆盖。重新加载只读取状态，不隐式刷新上游。

告警点击通过稳定 `account_id`、`metric_id` 和 `window_id` 定位。若目标被筛选隐藏，页面清除冲突筛选并说明原因；若账户已归档或不存在，显示明确回退信息。

## Responsive And Accessible Layout

- 在 390、768 和 1440 宽度下，核心操作无横向拖动、文本遮挡或按钮溢出。
- 固定工具栏、刷新按钮、状态徽标和数值单元使用稳定尺寸约束，动态内容不会改变布局。
- 键盘焦点环必须可见，标签页、按钮、筛选和输入框有合理顺序。
- 触控目标可用，不依赖 hover。图标按钮提供可访问名称或工具提示。
- 尊重 `prefers-reduced-motion`；历史滚动、告警强调等动画不能成为理解状态的前提。

## Rendering Rules

- 缺失、暂不提供、查询失败、真实零值和无限制使用不同文案。
- 超额窗口保留原始已用量和超额量；进度条最多 100%，但不能因此把状态降为正常。
- 过期快照显示真实最后成功时间及后台重试状态。
- 错误优先显示中文摘要，脱敏上游原文只放在诊断详情。
- 金额使用字符串传输和后端十进制格式化，避免二进制浮点中间舍入。
