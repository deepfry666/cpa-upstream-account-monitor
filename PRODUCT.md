# 产品说明

## Register

product

## Users

主要使用者是 CPA/CPAMP 的个人运维者。用户在多个官方 API、中转站、NewAPI、Sub2API 和滚动额度账户之间工作，需要在一个页面快速判断账户是否可用、当前余额或周期额度是多少，以及什么时候会重置。

## Product Purpose

“上游账户监控”是 CPA 的旁路运维工具。它独立读取 CPA 已配置的凭据和 Provider，查询各上游公开的余额或用量接口，统一成可解释的账户快照，并在 CPAMP 中提供可扫读的管理页面。它不参与模型路由、不修改请求、不修改凭据，也不依赖其他余额插件的数据库或状态。

成功标准是：用户不需要理解每家 Provider 的计费协议，就能准确区分货币余额、账户配额和周期额度；查询失败时不会看到虚假的 0 或百分比；异常账户、过期快照和重置时间都能被快速定位。

## Brand Personality

专业、克制、可信。页面应该像一个供长期使用的运维控制台，而不是营销页面或临时 Demo。信息密度可以高，但每个数字必须有明确语义。

## Anti-references

避免花哨渐变、赛博风暗色主题、巨型营销式指标卡、无意义的装饰图形、过度圆角和只用颜色表达状态。不要把余额、额度和健康状态压缩成一个无法解释的百分比。

## Design Principles

1. 先说明计费语义，再展示数字。
2. 失败不等于零值，旧数据必须明确标记为过期。
3. 页面首屏服务于判断和行动，而不是装饰。
4. Provider 细节隐藏在展开区，但不牺牲可追溯性。
5. 任何告警都要能回到具体账户、具体指标和具体时间。

## Accessibility & Inclusion

以中文为主，状态同时使用文字和颜色表达，满足键盘操作和可见焦点要求。支持窄屏查看，避免依赖 hover。尊重 `prefers-reduced-motion`，数字使用等宽数字特性以便比较。
