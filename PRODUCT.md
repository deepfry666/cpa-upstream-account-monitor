# 产品说明

## Register

product

## 产品定位

“上游账户监控”是 CPA / CPA Manager Plus 的旁路运维工具。它读取 CPA 已配置的宿主凭证和静态 Provider，独立查询上游公开的余额、额度、用量接口，统一成可解释、可追溯的账户快照。插件不参与模型路由，不修改认证、Provider 或请求处理逻辑，也不读取其他余额插件的数据库。

## 目标用户

主要使用者是在多个官方 API、NewAPI、Sub2API、OpenCode、Command Code 和滚动额度账户之间工作的个人运维者。用户需要在桌面或手机上快速回答：

- 账户现在是否可用，数据来自哪个时间点。
- 当前看到的是现金、账户配额、当前 Key 配额还是周期窗口。
- 哪个账户、哪个字段组或哪个窗口需要处理，失败发生在什么时候。
- 修改协议、PAT、代理或阈值后是否已经生效，升级或回滚是否安全。

## 成功标准

1. 打开页面立即看到最近一次持久化数据，不因上游慢请求出现空白页。
2. 现金、账户额度、Key 额度、订阅窗口和超额分别呈现，缺失、零值、未知和不支持不混用。
3. 配置保存原子且带修订冲突保护；轮询、切换页面和保存失败都不会丢正在编辑的草稿。
4. 账户身份、凭证、历史和显示顺序稳定；取消监控后旧请求不能恢复账户。
5. 所有网络请求可超时、可取消、受并发上限约束，失败不会阻塞其他账户或配置保存。
6. 后台同步、缓存 TTL、代理模式和宿主回调限制都按实际行为公开，不作不可验证的“全自动”声明。
7. 桌面、平板、手机和 CPAMP iframe 中，核心查看与配置操作均可完成，键盘焦点可见。
8. Hermes 可读取稳定的只读报告；新增字段支持过期、部分失败和字段组时间，旧 `schema_version` 路由保持兼容。

## 非目标

- 不执行充值、配额重置、路由切换、凭证轮换或供应商修改。
- 不把推理 Key 当管理 PAT，不猜测未知协议，不伪造官方额度或现金余额。
- 不在此次修复中重写为前端框架，不引入需要额外运行时的页面架构。
- 不把 `/api/v1/report?refresh=1` 改成新的写入接口；页面使用独立的异步刷新 API。

## 当前支持

支持 DeepSeek、智谱 / Z.ai、Moonshot、Kimi Coding、NewAPI、Sub2API、OpenCode Go、Command Code GOAT、OpenAI 兼容中转和 Codex API Key。数据模型保留余额、数量、窗口、字段组状态、诊断详情和结构化告警。

智谱 / Z.ai 当前只有可验证的套餐周期额度接口。官方现金余额接口不存在或无法验证时，产品明确展示限制并提供控制台入口；这不是待用套餐额度替代的字段。

## 关键产品规则

- `last_attempt_at` 表示本次查询尝试，`last_success_at` 表示最近成功数据时间，`generated_at` 只表示报告生成时间。
- 过期快照至少产生“需关注”，但不能掩盖更严重的账户故障。
- 账户状态按 `error > critical > warning > unknown > ok` 汇总，与处理顺序无关。
- 取消监控保留历史和上次快照归档；清除记录是独立、需确认的破坏性操作。
- 配置变更只有成功持久化后才触发查询刷新；磁盘失败返回独立警告。
- PAT 不进入 URL、日志、报告、缓存诊断或发布材料，接口只返回状态。

## 验收证据

发布候选需通过 Go 单元与集成测试、race、vet、Playwright 全部视口与交互用例、AMD64/ARM64 `c-shared` 构建，以及敏感信息扫描。36 项问题及限制的逐项映射见 `docs/IMPLEMENTATION_TEST_MATRIX.md`。
