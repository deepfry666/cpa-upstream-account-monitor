# 上游账户监控 0.5.0 实施与测试矩阵

本文对应《上游账户监控：完整修复实施方案》的 01-36 项，记录实现落点和可重复执行的自动化证据。插件技术 ID 仍为 `upstream-monitor`，动态库名仍为 `upstream-monitor.so`。

## 结论

- 01-08、10-36 已实现并通过 Go 单元/集成测试或 Playwright 场景覆盖。
- 09 是外部接口限制：智谱 / Z.ai 的套餐周期额度可查询，但没有经过验证、可供插件调用的公开现金余额接口。UI、README 和测试只声明“暂不支持自动查询现金余额”，不把套餐额度当作现金余额，也不声明该项已补齐。
- 最终发布前门槛为 `gofmt`、`git diff --check`、`go test -count=1 ./...`、`go test -race ./...`、`go vet ./...` 和完整 Playwright 套件。

## 逐项矩阵

| 项 | 状态 | 实现位置 | 自动化证据 |
| --- | --- | --- | --- |
| 01 · 修复位置型账户 ID 和账户串用 | 已实现 | `prefs.go`、`main.go`、`cache.go` | `TestMonitorPrefsIdentityStableAcrossReorder`、`TestMonitorPrefsIdentitySurvivesReload`、`TestMonitorPrefsMigratesPATAADToStableIdentity`、`TestMonitorPrefsMigratesUniqueLegacyPositionID`、`TestMonitorPrefsAmbiguousLegacyIDRequiresRelink`、`TestMonitorPrefsMissingLegacyIdentityBasisRequiresRelink`、`TestSnapshotStorePrunesRemovedAccounts` |
| 02 · 备注不再参与协议与目标地址选择 | 已实现 | `adapters.go`、`commandcode.go` | `TestAdapterDetectsCommandCode`、`TestAdapterMigratesStaleOpenCodeOverride`、`TestAdapterIgnoresDisplayNameAndUnrelatedBasePath`、`TestDeepSeekURLRejectsCustomHost` |
| 03 · 修正 NewAPI 单位与金额换算 | 已实现 | `adapters.go` | `TestNewAPIAmountUsesInstanceDisplaySettings`、`TestParseNewAPIQuotaUsesDisplayCurrencyRules`、`TestParseNewAPIQuotaShowsTokensWithoutCurrencyConversion`、`TestParseNewAPIQuotaUsesCustomCurrencySettings`、`TestParseNewAPIQuotaPreservesRawValueWhenDisplaySettingsAreIncomplete`、`TestNewAPIAmountHandlesZeroDecimalAndNegativeQuota`、`TestParseNewAPIQuotaStatusSupportsCustomAndKeepsUnknownTypes` |
| 04 · 修正百分比、窗口命名和顺序 | 已实现 | `adapters.go`、`model.go`、`ui.html` | `TestQuotaWindowFromPercentUsesExplicitUnits`、`TestParseOpenCodeSnapshotKeepsDefinedWindowOrder`、`TestParseZaiWindowNamesDoNotDependOnResponseOrder`、`TestParseZaiUsageLimits` |
| 05 · 分开套餐到期与窗口重置 | 已实现 | `model.go`、`adapters.go`、`ui.html` | `TestParseSub2APISeparatesExpiryFromWindowReset`、`TestParseNewAPIExpiryDoesNotBecomeReset`、Playwright `account details keep current quota visible while grouped views expose billing usage and diagnostics` |
| 06 · 保留超额窗口 | 已实现 | `adapters.go`、`main.go` | `TestQuotaWindowFunctionsPreserveOverage`、`TestParseSub2APISubscriptionKeepsDailyOverageDespiteHealthyWeekly`、`TestApplyThresholdsTreatsOverageAsCritical` |
| 07 · 增加现金金额阈值 | 已实现 | `model.go`、`prefs.go`、`main.go`、`ui.html` | `TestApplyThresholdsUsesCurrencyAndAccountCashRules`、`TestNormalizeCashThresholdsRejectsInvalidRanges`、`TestMonitorSettingsRouteSavesCashThresholds`、`TestMonitorSettingsSurviveRestart`、`TestProviderConfigRouteAtomicallySavesAccountCashThreshold`、`TestMonitorSettingsThresholdChangeReappliesSnapshotWithoutRefresh`、Playwright `monitor settings save currency cash thresholds`、`provider save includes an account cash threshold override`、`clearing an existing account cash override sends an explicit clear flag` |
| 08 · 正确识别过期或禁用 Key | 已实现 | `adapters.go`、`main.go`、`model.go` | `TestParseSub2APIExpiredKeyOverridesValidFlagAndKeepsQuota`、`TestParseSub2APIExpiredTimestampOverridesValidFlag`、`TestParseSub2APIInvalidKeyIsNotHealthy`、`TestAlertsForAccountIncludesUnavailableKeyCodes` |
| 09 · 补齐智谱现金余额的真实支持路径 | 外部限制，未声明已补齐 | `adapters.go`、`ui.html`、`README.md`、`README_EN.md` | `TestParseZaiSnapshotDeclaresCashBalanceUnsupported`、`TestParseZaiUnsupportedResponse`、Playwright `Z.ai cash balance limitation renders with console action`。通过条件是明确限制、保留套餐查询并提供控制台入口，不伪造现金金额。 |
| 10 · 按官方实际值展示 CommandCode 窗口 | 已实现 | `commandcode.go`、`model.go`、`ui.html` | `TestParseCommandCodeGOATSnapshot`、`TestParseCommandCodeUsesServerGrantWithoutPlanOverride`、`TestParseCommandCodeWithoutServerGrantDoesNotTreatRemainingAsTotal`、`TestParseCommandCodeProviderModeDoesNotFakeWindows`、`TestParseCommandCodeMapsExplicitMonthWindow`、`TestParseCommandCodeWithoutSubscriptionKeepsCredits`、Playwright `Command Code layout renders on mobile/tablet/desktop` |
| 11 · 实现缓存即时读取、后台刷新及 CPA 同步 | 已实现 | `main.go`、`background.go`、`cache.go`、`storage.go`、`ui.html` | `TestSnapshotStoreSavesVersionedCacheAndReloadsHistory`、`TestSnapshotStoreVersionedSaveReloadRoundTripsExactly`、`TestMonitorUIStateKeepsLastDirectoryWhenSyncFails`、`TestNewlyDiscoveredProviderIsNotMonitoredByDefault`、`TestBackgroundSchedulerStopsAndCancelsInflightRefresh`、`TestProviderViewsPreserveFirstSeenOrder`、`TestProviderViewsIncludeFirstSeenTime`、Playwright `navigation separates monitor settings from read-only data status` |
| 12 · 分清重新加载、刷新当前和刷新全部 | 已实现 | `main.go`、`background.go`、`ui.html` | `TestRefreshRouteWithAccountIDsSkipsOtherAccounts`、`TestRefreshRouteAsyncReturnsTrackableJob`、`TestRefreshRouteAsyncReportsPerAccountResult`、`TestRefreshRouteAsyncReportsSkippedFreshAccount`、`TestRefreshRouteRejectsUnknownScope`、`TestRefreshRouteRejectsUnknownAccountBeforeQueueing`、Playwright `single account refresh sends an explicit asynchronous request`、`refresh all sends the explicit all scope`、`reload only reads state and never starts a refresh` |
| 13 · 所有网络路径落实超时与取消 | 已实现 | `main.go`、`background.go`、`adapters.go`、`proxy.go` | `TestRefreshAccountsEnforcesAccountBudget`、`TestRefreshAccountsDoesNotSerializeDifferentAccounts`、`TestRefreshAccountsCoalescesConcurrentSameAccount`、`TestRefreshAccountsCancelsSlowResponseBodyAtAccountBudget`、`TestBackgroundSchedulerStopsAndCancelsInflightRefresh`、`TestQueryEndpointClosesIdleConnectionsAfterRequest` |
| 14 · 正确继承和覆盖代理 | 已实现 | `proxy.go`、`main.go`、`adapters.go` | `TestProviderViewsPreserveProxyInheritanceAndDirectOverride`、`TestQueryCandidateDirectOverrideBypassesInheritedProxy`、`TestQueryCandidateInheritsHostCredentialProxy`、`TestQueryCandidateCredentialDirectOverridesProviderProxy`、`TestQueryEndpointUsesSOCKS5Proxy`、`TestQueryEndpointUsesSOCKS5Authentication`、`TestQueryEndpointUsesHTTPProxyAuthentication`、`TestQueryEndpointRejectsUnsupportedProxySchemeWithoutDirectFallback`、`TestQueryEndpointProxyAuthenticationFailureDoesNotExposeCredentials`、`TestAccountProxyOverrideBeatsProviderProxy`、`TestAccountProxyURLOverrideBeatsProviderProxy`、`TestConfigureRejectsInvalidMonitorProxyMode`、`TestConfigureRejectsMonitorProxyURLModeWithoutURL`、`TestConfigureRejectsUnsupportedMonitorProxyScheme`、`TestConfigureRejectsMonitorProxyURLWithoutMode`、`TestConfigureRejectsMonitorProxyURLForDirectMode` |
| 15 · 保存查询配置后自动更新结果 | 已实现 | `main.go`、`prefs.go`、`ui.html` | `TestProviderConfigRefreshStartsAfterQueryConfigurationChanges`、`TestProviderConfigRouteReturnsTrackedRefreshJob`、`TestProviderConfigRouteNoteOnlyChangeDoesNotRefresh`、Playwright `provider save sends revision and explicit PAT actions`、`provider save result remains visible after state refresh` |
| 16 · 防止取消监控后旧请求恢复账户 | 已实现 | `main.go`、`cache.go` | `TestRefreshAccountsDiscardsSnapshotAfterMonitoringCanceled`、`TestCleanupInvalidatesInFlightQueryResults`、`TestBuildReportExcludesExplicitlyUnmonitoredAccounts`、`TestSnapshotStorePruneKeepsHistoryForRemovedAccount` |
| 17 · 修复缓存版本识别与重启恢复 | 已实现 | `cache.go`、`storage.go` | `TestSnapshotStoreSavesVersionedCacheAndReloadsHistory`、`TestSnapshotStoreRejectsUnknownFutureVersionWithoutChangingMemory`、`TestSnapshotStorePruneKeepsHistoryForRemovedAccount`、`TestSnapshotStoreLoadsNoVersionEntriesHistoryFormat`、`TestSnapshotStoreLoadsLegacyAccountMapFormat`、`TestSnapshotStoreTruncatedLoadPreservesFileAndMemory`、`TestSnapshotStoreVersionedSaveReloadRoundTripsExactly` |
| 18 · 配置保存原子化并报告持久化错误 | 已实现 | `prefs.go`、`storage.go`、`main.go` | `TestMonitorPrefsBatchFailureDoesNotPartiallyCommit`、`TestMonitorPrefsConcurrentBatchesPreserveBothChanges`、`TestMonitorPrefsConcurrentCASRejectsStaleWriter`、`TestProviderConfigRouteRejectsStaleRevisionWithoutChangingState`、`TestProviderConfigRouteRejectsInvalidBatchWithoutPartialCommit`、`TestProviderConfigRouteReportsSnapshotPersistenceWarning`、`TestProviderConfigRouteReportsPreferencePersistenceFailure`、`TestProviderConfigRouteAtomicallySavesPATWithoutEcho` |
| 19 · 明确 PAT 编辑的三种操作 | 已实现 | `prefs.go`、`main.go`、`ui.html` | `TestMonitorPrefsBatchPATActionsAreExplicit`、`TestMonitorPrefsEncryptsAndReloadsPAT`、`TestProviderConfigRouteAtomicallySavesPATWithoutEcho`、Playwright `provider save sends revision and explicit PAT actions`、`changing provider adapter reveals its authentication field immediately` |
| 20 · 密钥丢失时正确降级 | 已实现 | `prefs.go`、`main.go`、`storage.go` | `TestMonitorPrefsMissingKeyDoesNotOverwriteCiphertext`、`TestMonitorPrefsWrongKeyReportsDecryptError`、`TestMonitorPrefsMissingKeyStillSavesNonPATChanges`、`TestMonitorPrefsInvalidKeyStillLoadsNonPATState` |
| 21 · 统一 Sub2API 的自动和手动解析 | 已实现 | `adapters.go`、`model.go` | `TestParseSub2APIRateLimits`、`TestParseSub2APIBalanceIsNotPeriodPercent`、`TestParseSub2APIAllowsRemainingOnlyBalance`、`TestSub2APIUsageEndpointUsesSharedStatisticsParameters`、`TestApplySub2APIBillingResponseMarksPartialFailure`、`TestParseSub2APIQuotaLimitedIsPeriodQuota`、`TestParseSub2APISubscriptionWindows`、`TestParseSub2APISubscriptionUSDFieldsIgnoreTopLevelCurrency` |
| 22 · NewAPI 账户与 Key 独立查询 | 已实现 | `adapters.go`、`main.go` | `TestNewAPISnapshotKeepsAccountQuotaWhenKeyQueryFails`、`TestNewAPISnapshotKeepsKeyQuotaWhenAccountQueryFails`、`TestNewAPISnapshotMarksMissingManagementPAT`、`TestParseNewAPIQuotaMarksTokenScope` |
| 23 · 用确定规则汇总状态 | 已实现 | `main.go`、`model.go` | `TestBuildReportSummarizesKindsAndAlerts`、`TestBuildReportFromAccountsUsesSeverityOrderRegardlessOfOrder`、`TestBuildReportExcludesExplicitlyUnmonitoredAccounts`、`TestBuildReportStaleWarningDoesNotMaskAccountErrors`、`TestAlertsForAccountIncludesUnavailableKeyCodes` |
| 24 · 呈现部分失败和首次失败 | 已实现 | `main.go`、`model.go`、`adapters.go`、`ui.html` | `TestErrorSnapshotRecordsFirstAttempt`、`TestErrorSnapshotPreservesPreviousSuccessData`、`TestAccountSnapshotWithSuccessMarksOnlySuccessfulSectionsCurrent`、`TestSectionErrorKeepsPrimaryBalanceAndCreatesAlert`、`TestApplySub2APIBillingResponseMarksPartialFailure`、`TestNewAPISnapshotKeepsAccountQuotaWhenKeyQueryFails`、`TestNewAPISnapshotKeepsKeyQuotaWhenAccountQueryFails`、`TestCommandCodePartialFailurePreservesCredits`、Playwright `account details keep current quota visible while grouped views expose billing usage and diagnostics` |
| 25 · 统一中文、精度和时间 | 已实现 | `model.go`、`adapters.go`、`ui.html` | `TestQuotaAlertIdentifiesLowWindowAndThreshold`、`TestAlertUsesChineseSummaryAndKeepsDiagnostic`、`TestTimeValueParsesSupportedFormatsAndRejectsInvalidValues`、`TestDecimalStringKeepsSmallNonZeroValuesVisible`、`TestAlertsForAccountIncludesSectionFailure` |
| 26 · 重排首屏 | 已实现 | `ui.html` | Playwright `provider editing stays usable without horizontal scrolling at 390px/768px`、`Command Code layout renders on mobile/tablet/desktop`、`CPAMP iframe embedding uses the light/dark parent theme`；Go 侧报告字段通过 `TestProviderViewsIncludeFirstSeenTime` 提供稳定时间。 |
| 27 · 保留编辑草稿并处理冲突 | 已实现 | `ui.html`、`main.go`、`prefs.go` | Playwright `provider revision conflict keeps the draft and explains recovery`、`provider drafts survive tab changes`、`state refresh preserves provider and monitor settings drafts`、`unload is guarded only while provider drafts are dirty`；Go 侧 `TestProviderConfigRouteRejectsStaleRevisionWithoutChangingState` |
| 28 · 历史绑定账户并立即可见 | 已实现 | `main.go`、`cache.go`、`ui.html` | Playwright `late history responses never replace the selected account history`、`opening history identifies and focuses the selected account`、`history load failures stay in the selected account view`；Go 侧 `TestSnapshotStoreSavesVersionedCacheAndReloadsHistory` |
| 29 · 告警可跨筛选定位 | 已实现 | `ui.html` | Playwright `clicking an alert locates its metric and offers filter recovery`、`clicking an alert for an archived account explains the fallback`；Go 侧 `TestAccountAlertsUseStableIdentityIDs` 保证定位使用稳定 ID。 |
| 30 · 重做窄屏配置编辑布局 | 已实现 | `ui.html` | Playwright `provider editing stays usable without horizontal scrolling at 390px/768px`、`Command Code layout renders on mobile/tablet/desktop`、`keyboard navigation exposes a visible focus ring in management controls` |
| 31 · 保持列表顺序并支持快速比较 | 已实现 | `main.go`、`model.go`、`ui.html` | `TestProviderViewsPreserveFirstSeenOrder`、`TestProviderViewsIncludeFirstSeenTime`、`TestSnapshotStoreListHasStableAccountOrder`、Playwright `accounts keep first-seen order until the user chooses another sort` |
| 32 · 详情分组但不删功能 | 已实现 | `ui.html`、`model.go` | Playwright `account details keep current quota visible while grouped views expose billing usage and diagnostics`、`navigation separates monitor settings from read-only data status` |
| 33 · 明确数据状态与监控设置 | 已实现 | `main.go`、`prefs.go`、`ui.html` | `TestMonitorSettingsSurviveRestart`、`TestMonitorSettingsRouteSavesCashThresholds`、`TestMonitorSettingsThresholdChangeReappliesSnapshotWithoutRefresh`、Playwright `navigation separates monitor settings from read-only data status`、`monitor settings save currency cash thresholds` |
| 34 · 完整操作反馈与清除保护 | 已实现 | `main.go`、`cache.go`、`ui.html` | `TestCleanupInvalidatesInFlightQueryResults`、Playwright `single account refresh sends an explicit asynchronous request`、`refresh polls the job and reports its final result`、`provider save result remains visible after state refresh`、`provider save failure preserves every edited draft field`、`clearing an account snapshot requires explicit confirmation` |
| 35 · 正确记录失败发生时间 | 已实现 | `main.go`、`model.go`、`cache.go`、`ui.html` | `TestErrorSnapshotRecordsFirstAttempt`、`TestErrorSnapshotPreservesPreviousSuccessData`、`TestAccountSnapshotWithSuccessRecordsAttemptAndSuccess`、`TestAccountSnapshotWithSuccessMarksOnlySuccessfulSectionsCurrent`、Playwright `account details keep current quota visible while grouped views expose billing usage and diagnostics` |
| 36 · 明确端点路径和内部 HTTP 策略 | 已实现 | `adapters.go`、`commandcode.go`、`main.go` | `TestSub2APIUsageEndpointPreservesDeploymentPrefix`、`TestNewAPIManagementEndpointStripsInferenceSuffix`、`TestFixedEndpointAllowsInternalHTTPOnlyWhenExplicit`、`TestFixedEndpointNormalizesDeploymentPaths`、`TestConfigureRejectsInternalHTTPWithoutExplicitOptIn`、`TestDeepSeekURLRejectsCustomHost`、`TestManagementRegistrationUsesExactRoutes` |

## 最终门槛

以下命令均在仓库根目录执行；Playwright 使用仓库内嵌 `ui.html` 的静态服务，不读取或打包真实凭证。

```bash
gofmt -w *.go
git diff --check
go test -count=1 ./...
go test -race ./...
go vet ./...
NODE_PATH=/Users/jjjj/.npm/_npx/de56a0059759c86a/node_modules \
  /Users/jjjj/.npm/_npx/de56a0059759c86a/node_modules/.bin/playwright \
  test ui-smoke.spec.js --reporter=line
```

构建发布包时使用 `VERSION=0.5.0`、`GOOS=linux`，分别构建 `GOARCH=amd64` 和 `GOARCH=arm64`。`dist/`、生成的 `.so`/`.h`、缓存、截图和真实凭证均不进入 Git。
